{%
  set classes = [
  node.bundle|clean_class,
  node.isPromoted() ? 'is-promoted',
  node.isSticky() ? 'is-sticky',
  not node.isPublished() ? 'is-unpublished',
  view_mode ? view_mode|clean_class,
]
%}
<div {{ attributes.addClass(classes) }}>
  <span class="createdtime">{{ node.getCreatedTime()|date("d M Y") }}</span>

  <h4><a href="{{ node.field_os2web_ep_url.value }}" target="_blank">{{ label }}</a></h4>

  <div>
    {% if node.body.value %}
      {{ node.body.value|striptags|replace({'&nbsp;': ' '})|slice(0, 256) ~ '...' }}
    {% else %}
      {{ content }}
    {% endif %}
  </div>

  <br>

  <div class="search-content-type">
    {{ node.type.entity.label }} {% trans %}from{% endtrans %}: {{ node.field_os2web_ep_domain.value }}
  </div>
</div>
