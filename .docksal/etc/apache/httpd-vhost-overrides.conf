
<IfModule mod_dav_lock.c>
    DAVLockDB "/var/www/logs/davlock.db"
</IfModule>

<IfModule mod_dav_fs.c>

    Alias /webdav "/var/www/webdav"

    <Directory /var/www/webdav>
        Dav On
        Require all granted
        AllowOverride All
        Options +Indexes
    </Directory>

</IfModule>