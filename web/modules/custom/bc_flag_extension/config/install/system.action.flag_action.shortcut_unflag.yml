langcode: da
status: true
dependencies:
  config:
    - flag.flag.shortcut
  module:
    - flag
id: flag_action.shortcut_unflag
label: 'Unflag this item'
type: node
plugin: 'flag_action:shortcut_unflag'
configuration:
  flag_id: shortcut
  flag_action: unflag
