langcode: da
status: true
dependencies:
  config:
    - flag.flag.shortcut
  module:
    - flag
id: flag_action.shortcut_flag
label: 'Flag this item'
type: node
plugin: 'flag_action:shortcut_flag'
configuration:
  flag_id: shortcut
  flag_action: flag
