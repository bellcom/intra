diff --git a/src/Form/UserDeleteReassignConfigForm.php b/src/Form/UserDeleteReassignConfigForm.php
index 4460fa9fda1a1e86f558512105acb9bf5981f0f7..600c3a4d626b15079806e682df402bdc3069ffd4 100644
--- a/src/Form/UserDeleteReassignConfigForm.php
+++ b/src/Form/UserDeleteReassignConfigForm.php
@@ -5,6 +5,7 @@ namespace Drupal\user_delete_reassign\Form;
 use Drupal\Component\Utility\Html;
 use Drupal\Core\Form\ConfigFormBase;
 use Drupal\Core\Form\FormStateInterface;
+use Drupal\user\Entity\Role;
 
 /**
  * Configure user delete reassign settings for this site.
@@ -33,9 +34,9 @@ class UserDeleteReassignConfigForm extends ConfigFormBase {
   public function buildForm(array $form, FormStateInterface $form_state) {
     $config = $this->config('user_delete_reassign.settings');
 
-    $roles = user_role_names(TRUE);
-    foreach ($roles as $id => $val) {
-      $roles[$id] = Html::escape($val);
+    $roles = [];
+    foreach (Role::loadMultiple() as $id => $role) {
+      $roles[$id] = Html::escape($role->label());
     }
 
     $form['role_filter'] = [
diff --git a/user_delete_reassign.info.yml b/user_delete_reassign.info.yml
index 3dc7fc86b2ab198734e864d29e4f09f8ce75c493..e83f95bab94a5731afe405aee73ee26602a4b34c 100644
--- a/user_delete_reassign.info.yml
+++ b/user_delete_reassign.info.yml
@@ -1,5 +1,5 @@
 name: 'User Delete Reassign'
 type: module
 description: 'Alters the user deletion form allowing users to reassign their content to some other existing users.'
-core_version_requirement: ^9 || ^10
+core_version_requirement: ^9.3 || ^10 || ^11
 configure: user_delete_reassign.settings
diff --git a/user_delete_reassign.module b/user_delete_reassign.module
index fd6af9310f1a62486605528e559c8e7f4aa5b2e0..eb2e80609047b2ddf2c3d91e3a7b2de0990bd4ef 100644
--- a/user_delete_reassign.module
+++ b/user_delete_reassign.module
@@ -119,6 +119,7 @@ function user_delete_reassign_user_cancel($edit, $account, $method) {
       $new_user = User::load($edit['user_cancel_reassign_to']);
       Drupal::moduleHandler()->loadInclude('node', 'inc', 'node.admin');
       $nodes = \Drupal::entityQuery('node')
+        ->accessCheck(FALSE)
         ->condition('uid', $account->id())
         ->execute();
       node_mass_update($nodes, ['uid' => $new_user->id()], NULL, TRUE);
