{%
  set classes = [
  node.bundle|clean_class,
  node.isPromoted() ? 'is-promoted',
  node.isSticky() ? 'is-sticky',
  not node.isPublished() ? 'is-unpublished',
  view_mode ? view_mode|clean_class,
  node.field_os2web_hearings_tender.target_id == 4563 ? 'hearing-tender':'hearing-type'
]
%}
{% set timestamp = node.field_os2web_m_start_date.value|date('U') %}
<div {{ attributes.addClass(classes) }}>
  {{ content.field_os2web_hearings_heading }}
  {{ content.field_os2web_hearings_intro }}
  {{ content.body }}
  {{ content.field_os2web_hearings_attac_doc }}

  {% if not content['#is_decision'] %}
  {{ content.field_os2web_hearings_reply }}

  <div class="box">
    <div class="box__body">
      <div class="row">
        <div class="col-12 col-md-auto">
          {% if node.field_os2web_hearings_tender.target_id != "4563" %}
          {{ content.field_os2web_hearings_end_date }}
          {% endif %}
        </div>
        <div class="col-12 col-md-auto">
          {{ content.field_os2web_hearings_start_date }}
        </div>
        <div class="col-12 col-md-auto">
          {% if not content['#closed'] %}
            {{ content.links }}
          {% endif %}
        </div>
      </div>
    </div>
  </div>

  {% endif %}
  <br />

  {# Link back to result overview. Hardcoded! #}
  <div class="node__links">
    <a href="{{ content.destination }}" role="button">
      {{ 'Tilbage' | trans }}
    </a>
  </div>

  <br><br>
</div>
